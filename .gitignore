# Real config holds secrets — never commit it. Ship config.example.toml instead.
config.toml
prowlarr-media/config.toml
**/config.toml

# Cookies / session artifacts
*.cookie
*.cookies

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Internal design/agent notes — not part of the shared skill
.aidocs/

# Editor / OS
.DS_Store
*.swp
