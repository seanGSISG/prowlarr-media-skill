# Real config holds secrets — never commit it. Ship config.example.toml instead.
config.toml
prowlarr-media/config.toml
**/config.toml

# Cookies / session artifacts
*.cookie
*.cookies

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Internal design notes — not part of the shared skill (INSTALL.md IS shared)
.aidocs/design.md

# Editor / OS
.DS_Store
*.swp
